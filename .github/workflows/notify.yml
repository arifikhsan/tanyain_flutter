name: Notify
on: push
jobs:
  notify:
    name: Notify
    runs-on: ubuntu-latest
    steps:
      - name: Telegram Notify
        uses: appleboy/telegram-action@0.0.7
        with:
          to: ${{ secrets.TELEGRAM_TO }}
          token: ${{ secrets.TELEGRAM_TOKEN }}
          message: ${{ secrets.GITHUB_ACTOR }} $${{ secrets.GITHUB_EVENT_NAME }} ヾ(≧▽≦*)o  o((>ω< ))o
